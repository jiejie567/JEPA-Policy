# JEPA-Policy training-layer dependencies installed only in venvs/robocasa.
# Simulator and LeRobot pins remain in robocasa_requirements.txt.
hydra-core==1.3.2
loguru==0.7.3
numcodecs==0.16.3
zarr==3.1.3
