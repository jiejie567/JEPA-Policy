# @package _global_
defaults:
  - libero_mip_future8_joint_smoke
  - _self_

optimization:
  # Architecture control: the future slot/head and both joint passes remain,
  # but the future objective contributes exactly zero to parameter updates.
  future_embed_loss_weight: 0.0

log:
  log_dir: "logs/mug_mug_future8_joint_action_only_control"
  exp_name: "mug_mug_future8_joint_action_only_control"
